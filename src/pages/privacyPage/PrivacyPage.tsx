import React from 'react';
import { Helmet } from 'react-helmet';

import { SEO_DESCRIPTIONS } from 'constants/Messages';

import {
  HeaderText,
  PageBody,
  PageBodyHeader,
  PageBodyParagraph,
  PageContentWrapper,
  PageHeader,
  PageWrapper,
} from './styles/PrivacyPage';

const PrivacyPage = () => {
  throw new Error('Error loading privacy page data');
};

export default PrivacyPage;
